// XaiDashboard — retired in favor of inline Recharts in PatientPortal
// Kept as stub to avoid import errors from any legacy references
import React from 'react';
const XaiDashboard = () => null;
export default XaiDashboard;
