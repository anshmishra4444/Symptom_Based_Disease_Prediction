// LiveLogic — retired in favor of SocketContext-based InferenceWidget
// Kept as stub to avoid import errors from any legacy references
import React from 'react';
const LiveLogic = () => null;
export default LiveLogic;
